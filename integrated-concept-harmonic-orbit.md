# Harmonic Orbit

一个围绕 `短循环和声` 的可玩重配器物。它把 `Borrow Well` 的色彩借用、`Cadence Orbit` 的终止引力、`Fork Mirror` 的分叉比较，以及 `Turntable Steps` 的短周期重排整合成同一个交互闭环：用户先给出一个两到八码的短片段，再围绕一个“回到哪里、怎么回去、有哪些替代路”持续旋转、试探、比较，最后收敛出一个真正可用的版本。

## 它是什么

`Harmonic Orbit` 不是通用和弦编辑器，而是一个专门处理 `短周期回环` 的数字物件。

它解决的不是“有哪些和弦可以用”，而是这三个更具体的问题：

- 同一个目标和声，能不能有更有颜色的接近方式
- 同一个 turnaround / tag / vamp，怎样变得更黏、更狡黠或更悬而不决
- 当几个 reharm 版本都成立时，怎样快速用耳朵选出最像这段音乐的一条

所以它的最小单位不是单个 chord，也不是整首 form，而是一个 `2-8 小节的循环片段`。用户永远在和一个会循环、会回来的对象互动。

## 核心隐喻

这个物件的核心隐喻是：

- 中心是 `落点`：片段最终想回到的和声或功能区域
- 外圈是 `路径`：通向落点的短周期步骤
- 井壁是 `色层`：同一功能可以借用的不同和声颜色
- 镜面是 `分叉`：几个成立但气质不同的版本并列试听

四个概念在这里不是四个模式，而是同一个界面的四个部分。用户不会先“进入 Borrow Well 模式”再“切到 Fork Mirror 模式”，而是一直在同一个回环对象上操作。

## 一句话定义

一句话定义：一个用于塑造短循环和声重力的屏幕原生器物，用户通过旋转回环、抽取借用色彩、插入回返步骤，并把几条替代路径并列试听，最终找到最像这段音乐的 landing path。

## 适合谁

- 会写和弦、会听功能、已经不满足于 chord list 的乐手
- 想改写标准曲 turnaround、intro、tag、ending vamp 的编曲者
- 想快速测试 reharm 方向，但不想打开完整 DAW 工作流的人

## 核心交互

### 1. 放入一个回环

用户先输入一个 `2-8 小节` 片段，方式可以非常简单：

- 键盘输入和弦符号
- 从预设片段开始
- 导入一小段已有 progression

输入后，片段会变成一个 `环形轨道`。每一拍或每半小节对应一个小台阶，台阶沿圆环排布，能立即循环播放。

### 2. 指定落点

用户点击环上的某一个和弦，把它设为 `落点`。这个落点会成为视觉与听觉的重力中心。

一旦落点设定，系统会把前面的若干步视为 `approach zone`，也就是“通往这里的路”。后续所有修改都优先发生在这个区域，不去碰整段别处内容。

这一步吸收了 `Cadence Orbit` 的核心：重点不是改整段，而是改“如何到达”。

### 3. 抽取色层

用户将 approach zone 里的某一步向内拖入圆心附近的 `色层井`，横向划过不同色层，就会听到这一格的功能替代：

- 原位功能
- parallel minor borrow
- backdoor family
- tritone family
- diminished passing
- lydian / altered dominant color

每次只替换当前一步的“和声颜色”，不直接改掉整条路径。这样它保留了 `Borrow Well` 的液态试听感，但落地得更具体。

### 4. 调整回返台阶

色彩选定后，用户可以继续编辑 approach zone 的台阶：

- `旋转`：改变某一步出现的时值位置
- `升降`：把一步变成更强或更弱的到达
- `翻面`：把某一步改成 passing / link / delay 类型
- `插步`：在两步之间插入一个短暂 passing dominant、side-slip 或 diminished link

这一步直接承接 `Turntable Steps`，但它不再是一个单独机械玩具，而是“被落点重力约束的步骤编辑”。

### 5. 生成分叉镜

每当用户满意于一条路径，可以把它 `拉出一面镜子`。镜子里保存的是当前 approach zone 的一个版本。

每面镜子只允许代表一种清晰思路，例如：

- 更蓝一点
- 更晚落地
- 更像标准曲
- 更悬着不 resolve
- 更适合 vamp 循环

用户最多保留 `4` 面镜子，环形主体持续循环，点击任意镜面即可无缝切换到对应版本。

这一步把 `Fork Mirror` 变成真正服务于决策的“版本栏”，而不是一个独立对比玩具。

## 为什么这个整合是自洽的

这四个原概念其实共享同一个问题框架：

- `Borrow Well` 回答：这一步可以换成什么颜色
- `Turntable Steps` 回答：这些步子怎么重新排布
- `Cadence Orbit` 回答：它们如何被同一个落点吸进去
- `Fork Mirror` 回答：几种路里到底哪条更像这段音乐

它们合并后的自然单位就是 `landing path`。

也就是：

- 不是编辑整首曲子
- 不是抽象研究和弦库
- 不是无上下文地生成 reharm
- 而是针对一个短循环片段，持续雕刻“回去的那条路”

这让产品目标非常集中，也让原型实现边界清晰。

## 界面骨架

界面只需要 4 个区域，就已经足够成立：

### A. 中央环轨

- 显示整个 2-8 小节循环
- 台阶大小表示时值
- 当前播放头沿环运动
- 落点被高亮成重力节点

### B. 色层井

- 位于环中心或偏内侧
- 只在用户拖某个台阶进入时展开
- 展示 5-6 个色层，不做海量理论菜单

### C. 镜面栏

- 位于右侧或下方
- 最多 4 个分叉版本
- 每个镜子显示一句标签和简化路径形状

### D. 听觉控制

- 循环开关
- 固定鼓点或 click
- A/B 无缝切换
- “只听 approach zone”
- “跳过落点前一拍”

这已经足够形成完整闭环，不需要更多模式页。

## 用户一次典型使用

例子：用户拿一个 `| Dm7 G7 | Cmaj7 A7 | Dm7 G7 | Cmaj7 |` 的四小节片段，想让最后一次回到 `Cmaj7` 更讲究。

1. 用户把最后一个 `Cmaj7` 设为落点。
2. 系统把前面的 `Dm7 G7` 识别为 approach zone。
3. 用户把 `G7` 拖进色层井，试出 `Db7`、`Ab7 -> G7`、`Bdim / G` 这些不同家族。
4. 用户插入一个短台阶，让原本两步的 approach 变成 `Dm7 | Db7 C7 | Cmaj7`。
5. 用户再拉出第二个版本，把它改成更拖延的 `Dm7 G7(b9) | Ab7 G7 | Cmaj7`。
6. 两个版本都保留为镜面，循环播放，同样的鼓点不变。
7. 用户快速切换，最后决定保留那个“先偏航再回正”的版本。

整个过程始终围绕一个目标点，不需要跳出到表格、列表或完整 piano roll。

## 真正能落地的 MVP

如果只做第一版，范围应当非常克制：

- 只支持 `2-4 小节` 循环
- 只支持每拍或每半小节一个 harmonic step
- 只支持 6 类替代家族
- 只允许编辑 `1 个落点` 前的 `2-4 步`
- 只保留 `最多 4 个镜面版本`
- 只做钢琴和简单 comp 音色，不做复杂配器
- 只做符号输入与试听，不做乐谱排版

MVP 的目标不是“足够专业到替代 DAW”，而是：

- 让用户在 1 分钟内做出 3 个可信版本
- 在 10 秒内听懂每个版本的落地气质
- 真能帮助用户选出一个更好的 turnaround / tag / vamp 回返

## 关键规则

为了保持这个概念不散掉，需要几条硬规则：

- 所有修改都围绕 `一个落点` 展开
- 所有色彩替代都先作用于 `单一步骤`
- 所有分叉版本都必须有一个明确标签
- 不允许在一个镜面里同时改太多层，否则比较失真
- 默认循环播放，停下来只是辅助，不是主状态

这些规则保证它像一个器物，而不是缩水版编曲软件。

## 为什么它有专业价值

- 它把 reharm 从“理论选择题”变成“落地路径设计”
- 它特别适合处理 jazz 里最值钱、但也最容易被写平的局部：`turnaround / tag / vamp / cadence`
- 它天然支持快速 audition，而不是改完一个版本再手动复制工程
- 它鼓励用户通过耳朵判断“回去的姿态”，这比单纯看 chord symbol 更接近真实音乐决策

## 为什么它仍然像玩具

- 环轨本身有机械感和手感
- 色层井提供液态抽取的惊喜
- 镜面切换带来即时的“这条路更对”体验
- 用户总是在拨动同一个小宇宙，而不是管理一堆参数页

## 边界

这个方案刻意不做这些事：

- 不处理整首 form 编排
- 不做主旋律生成
- 不做复杂节奏编曲
- 不做完整配器与混音
- 不做理论课程式解释系统

它只做一件事：`让短循环和声的回返路径变得更有颜色、更有重力、也更容易被比较和选定。`
