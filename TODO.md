# Harmonic Orbit TODO (Pending Only)

> 说明：本文件仅保留待办。已完成历史请看 `PROGRESS.md`，高层需求请看 `PRD.md`。

## P0 Stabilization
- [ ] Run first full checklist report generated by `scripts/smoke_checklist.sh` across Chrome/Safari/Edge
- [ ] Verify Web Audio behavior in Chrome, Safari, and Edge
- [ ] Verify `<dialog>` fallback behavior and keyboard focus handling
- [ ] Verify `localStorage`, JSON export, and JSON import with real session data
- [ ] Test full happy path end-to-end: demo loop -> edit -> audition -> pin -> promote -> compare -> save -> restore

## P1 Musical Usefulness
- [ ] Tighten chord parser coverage for more realistic jazz spellings
- [ ] Improve candidate generation so suggestions depend more explicitly on beat role and landing context
- [ ] Add better handling for slash chords and bass-directed voice leading
- [ ] Add stack-level summary heuristics so new stacks can start with a smart default label/note
- [ ] Add richer cadence audition modes beyond `step -> landing`, especially for two-step approach motion

## P1 Mirror / Stack Workflow

## Technical Cleanup
- [ ] Break `app.js` into smaller modules once behavior stabilizes
- [ ] Add a small internal test harness for parser, role detection, and diff generation
- [ ] Centralize mirror and audition serialization helpers
- [ ] Audit duplicated timing constants across compare and audition flows

## Major Version Plan

### Version v0.10-dev — Stabilization & Acceptance Prep
**Goal:** 进入可验收状态，关闭 P0 稳定性风险。  
**Major deliverables:**
- 完成跨浏览器稳定性验证（Chrome/Safari/Edge）
- 完成 `<dialog>` 与键盘焦点行为验证
- 完成 `localStorage` + JSON 导入导出真实数据验证
- 完成端到端 happy path 回归清单
- 执行首轮 checklist 实测并沉淀问题单

**Acceptance checks:**
- [ ] 浏览器 smoke 记录齐全
- [ ] 无阻断级交互问题
- [ ] 会话数据恢复与导入导出成功率稳定

### Version v1.0 — Musical Intelligence Upgrade
**Goal:** 提升音乐价值，减少“技术可用但音乐不够聪明”的问题。  
**Major deliverables:**
- parser 覆盖更真实 jazz spelling
- 候选生成更 role/context-aware
- slash/bass 导向语义补齐
- 两步 approach cadence audition 模式

**Acceptance checks:**
- [ ] 至少 3 组典型 jazz 走向通过手工对比验证
- [ ] 候选解释与听感基本一致

## Version Decision (Current)
- Current recommendation: **v0.10-dev**（继续开发）
- Upgrade to **v1.0** only when: no P0 blockers + acceptance checks fully green.

## Open Questions
- [ ] Should `Pull` remain exploration-only or become first-class saved compare axis?
- [ ] Should mirror groups stay auto-derived from `step + axis`, or become user-defined collections?
- [ ] Should app remain pure static prototype, or move to structured frontend app?
